#include "CjtUsuaris.hh"
#include "Curs.hh"
using namespace std;

int main() {
    string op;
    int P;
    for(int i = 0; i < P; ++i) {

    }
    int Q;
    for(int i = 0; i < Q; ++i) {
        
    }
    int N;
    for(int i = 0; i < N; ++i) {
        
    }
    int M;
    for(int i = 0; i < M; ++i) {
        
    }

    cin >> op;
    while(op != "fin") {
        
        if(op ==)

        cin >> op;
    }
}